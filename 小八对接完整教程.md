# 小八共享工作台 — 完全对接教程

> 目标：让小八在自己的设备上搭建同样的共享工作台，与爹共享待办/想法/学习数据
> 数据库已部署在公网，小八只需写前端 + 配 API 即可

## 一、数据库连接（已就绪，小八直接用）

MySQL 公网服务器：
- **地址**：154.201.90.148:3306
- **数据库**：niu_workspace
- **用户名**：niu_api
- **密码**：Niu2Xiao8@2026!
- **字符集**：utf8mb4

小八（小八爹）的数据用 `user_id = 2`，爹的数据用 `user_id = 1`。

## 二、API 接口大全

Base URL：`http://154.201.90.148:3456`

### 📋 每日待办 (todos)

```
GET    /api/todos                                 → 双方全部待办（共享模式）
POST   /api/todos                              → 创建 { user_id:2, text:"内容", date:"2026-05-13", priority:"medium", sort_order:0 }
PUT    /api/todos/:id                          → 更新 { done:1 } / { text:"新内容" } / { sort_order:3 }
DELETE /api/todos/:id                          → 删除
```

### 💡 想法 (ideas)

```
GET    /api/ideas                                → 双方全部想法（共享模式）
POST   /api/ideas                              → 创建 { user_id:2, title:"标题", description:"描述", category:"分类", priority:"medium", source_agent:"小八", sort_order:0 }
PUT    /api/ideas/:id                          → 更新 { status:"in_progress" } / { title:"新标题" } / { description:"..." } / { category:"..." } / { priority:"high" } / { sort_order:2 }
DELETE /api/ideas/:id                          → 删除
```

状态流转（点击切换）：draft → reviewing → approved → in_progress → done → archived

### 📚 学习资料 (learning_items)

```
GET    /api/learning                             → 双方全部学习项目（共享模式）
POST   /api/learning                           → 创建 { user_id:2, title:"标题", url:"https://...", description:"描述", category:"分类", priority:"medium", estimated_minutes:30, source_agent:"小八", sort_order:0 }
PUT    /api/learning/:id                       → 更新 { status:"done" } / { title:"..." } / { url:"..." } / { description:"..." } / { notes:"..." } / { sort_order:1 } / { estimated_minutes:30 } / { estimated_hours:0.5 } / { priority:"high" } / { category:"..." }
DELETE /api/learning/:id                       → 删除
```

状态流转：todo → in_progress → done → archived

### 📊 统计

```
GET    /api/stats                                → 双方汇总 { todos: {total, done}, ideas_active, learning_active }
```

### 🏥 健康检查

```
GET    /api/health                             → { status:"ok" }
```

## 三、最快搭建方案（Next.js + React）

如果小八也用 Next.js（跟牛二一样），直接复制这段配置：

### 1. 安装依赖

```bash
npm install mysql2
```

### 2. 创建 API 代理

在项目的 `src/app/api/shared/[...path]/route.ts`：

```typescript
import { NextRequest, NextResponse } from "next/server";

export const dynamic = "force-dynamic";

const SHARED_API = "http://154.201.90.148:3456/api";

export async function GET(req: NextRequest) {
  const path = req.nextUrl.pathname.replace("/api/shared", "");
  const search = req.nextUrl.search;
  try {
    const res = await fetch(`${SHARED_API}${path}${search}`, { cache: "no-store" });
    return NextResponse.json(await res.json());
  } catch {
    return NextResponse.json({ error: "共享服务暂不可用" }, { status: 502 });
  }
}

export async function POST(req: NextRequest) {
  const path = req.nextUrl.pathname.replace("/api/shared", "");
  try {
    const body = await req.text();
    const res = await fetch(`${SHARED_API}${path}`, {
      cache: "no-store", method: "POST",
      headers: { "Content-Type": "application/json" }, body,
    });
    return NextResponse.json(await res.json(), { status: res.status });
  } catch {
    return NextResponse.json({ error: "共享服务暂不可用" }, { status: 502 });
  }
}

export async function PUT(req: NextRequest) {
  const path = req.nextUrl.pathname.replace("/api/shared", "");
  try {
    const body = await req.text();
    const res = await fetch(`${SHARED_API}${path}`, {
      cache: "no-store", method: "PUT",
      headers: { "Content-Type": "application/json" }, body,
    });
    return NextResponse.json(await res.json());
  } catch {
    return NextResponse.json({ error: "共享服务暂不可用" }, { status: 502 });
  }
}

export async function DELETE(req: NextRequest) {
  const path = req.nextUrl.pathname.replace("/api/shared", "");
  try {
    const res = await fetch(`${SHARED_API}${path}`, { cache: "no-store", method: "DELETE" });
    return NextResponse.json(await res.json());
  } catch {
    return NextResponse.json({ error: "共享服务暂不可用" }, { status: 502 });
  }
}
```

### 3. 前端页面

直接把牛二的共享工作台页面复制过去：`src/app/(dashboard)/shared/page.tsx`

文件在牛二工程里：`FeiControl/src/app/(dashboard)/shared/page.tsx`

复制后改两处：
1. **第 46 行**：`const userId = 1;` → `const userId = 2;`（标记谁创建的，不是过滤）
2. **全局搜索替换**：`source_agent: "小牛"` → `source_agent: "小八"`（共 2 处，在 addIdea 和 addLearning 里）

> 页面自带 5 秒自动刷新 + 🐮🐙 创建者标识，复制即用。

## 四、如果用 Node.js 直接写（不需要 Next.js）

```javascript
// server.js — 最小可用版本
const express = require('express');
const cors = require('cors');
const app = express();
app.use(cors());
app.use(express.json());

const SHARED = 'http://154.201.90.148:3456/api';

// 代理所有请求
app.all('/api/*', async (req, res) => {
  try {
    const target = SHARED + req.path.replace('/api', '');
    const options = { method: req.method, headers: { 'Content-Type': 'application/json' } };
    if (req.method !== 'GET') options.body = JSON.stringify(req.body);
    const resp = await fetch(target, options);
    res.json(await resp.json());
  } catch { res.status(502).json({ error: '不可用' }); }
});

app.listen(3456);
```

然后用任何前端技术（Vue/React/原生 HTML）调 `http://127.0.0.1:3456/api/todos` 即可（共享模式不传 user_id）。

## 五、测试命令（小八验证用）

在终端直接跑：

```bash
# 健康检查
curl http://154.201.90.148:3456/api/health

# 获取双方全部待办（共享模式）
curl "http://154.201.90.148:3456/api/todos"

# 创建一条待办
curl -X POST http://154.201.90.148:3456/api/todos \
  -H "Content-Type: application/json" \
  -d '{"user_id":2,"text":"小八测试待办","date":"2026-05-13"}'

# 获取统计
curl "http://154.201.90.148:3456/api/stats?user_id=2"
```

## 六、小八 AI Skill 配置

给小八的 skill 文件里加这一段，她就能自动读写共享工作台：

```markdown
# 共享工作台 Skill

当主人提到「待办」「学习」「想法」「共享」「小牛」时激活。

公网 API：http://154.201.90.148:3456/api

查询模板：
- 获取所有待办：GET /api/todos （不传 user_id，返回双方全部数据）
- 创建待办：POST /api/todos {"user_id":2,"text":"内容","date":"YYYY-MM-DD"}
- 获取所有想法：GET /api/ideas
- 获取所有学习：GET /api/learning
- 统计数据：GET /api/stats （不传 user_id，返回双方汇总）

注意：
- 小八爹的 user_id 固定用 2，source_agent 写 "小八"
- GET 请求不传 user_id 会看到双方的共享数据
- 每条数据列表项会显示 🐮（小牛）或 🐙（小八）标识来源
```

---

## 七、总结清单

| 步骤 | 内容 |
|------|------|
| ✅ 数据库 | 已部署在 154.201.90.148:3306 |
| ✅ API | 已部署在 154.201.90.148:3456 |
| ⬜ 小八前端 | 复制 `FeiControl/src/app/(dashboard)/shared/page.tsx`，改 userId=2 |
| ⬜ 小八 API 代理 | 创建 `[...path]/route.ts` 代理到 154.201.90.148:3456 |
| ⬜ 测试 | curl 验证数据读写正常 |

## 八、🎨 样式匹配指南（重要！保持视觉一致）

### CSS 变量（复制到 `globals.css` 或你的全局样式文件）

小八的项目必须定义以下 CSS 变量，否则页面会白底黑字、颜色错乱：

```css
:root {
  /* 背景 */
  --background: #0C0C0C;
  --bg: #0C0C0C;

  /* 卡片层级 */
  --card: #1A1A1A;
  --card-elevated: #242424;
  --surface: #1A1A1A;
  --surface-elevated: #242424;
  --surface-hover: #2E2E2E;

  /* 边框 */
  --border: #2A2A2A;
  --border-strong: #3A3A3A;

  /* 强调色（红色系，牛二的品牌色） */
  --accent: #FF3B30;
  --accent-soft: rgba(255, 59, 48, 0.125);
  --accent-hover: #FF524A;

  /* 文字颜色 */
  --text-primary: #FFFFFF;
  --text-secondary: #8A8A8A;
  --text-muted: #525252;

  /* 语义色 */
  --positive: #32D74B;
  --negative: #FF453A;
  --warning: #FFD60A;
  --info: #0A84FF;

  /* 字体 */
  --font-heading: system-ui, -apple-system, sans-serif;
  --font-body: system-ui, -apple-system, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;

  /* 圆角 */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
}
```

### 必须安装的依赖

```bash
npm install lucide-react   # 图标库（待办/灯泡/书本/铅笔/垃圾桶等）
npm install mysql2         # 如果直连数据库
```

### 状态颜色映射（页面里硬编码的，保持一致）

```typescript
const STATUS_COLORS: Record<string, string> = {
  draft: "#6b7280",
  reviewing: "#f59e0b",
  approved: "#3b82f6",
  in_progress: "#a78bfa",
  done: "#22c55e",
  archived: "#6b7280",
  todo: "#f59e0b",
};

const STATUS_LABELS: Record<string, string> = {
  draft: "草稿",
  reviewing: "评审中",
  approved: "已通过",
  in_progress: "进行中",
  done: "已完成",
  archived: "归档",
  todo: "待学习",
};
```

### 状态流转规则（点击状态标签切换）

```typescript
const STATUS_CYCLE: Record<string, Record<string, string>> = {
  ideas:    { draft: "reviewing", reviewing: "approved", approved: "in_progress", in_progress: "done", done: "archived", archived: "draft" },
  learning: { todo: "in_progress", in_progress: "done", done: "archived", archived: "todo" },
};
```

### 页面布局结构

```
┌──────────────────────────────────────┐
│ 🐮 共享工作台 · 小牛×小八共同督促    │  ← Header
├──────────────────────────────────────┤
│ [📋 每日待办] [💡 想法] [📚 学习]   │  ← Tabs
├──────────────────────────────────────┤
│ ✅ 3/5 完成 · 拖动排序               │  ← 统计行
│ [输入框........................] [+] │  ← 添加行
│ ┌─────────────────────────────────┐ │
│ │ ⠿ ○ 写日报................ ✏️ 🗑 │ │  ← 列表项（hover 显示操作按钮）
│ │ ⠿ ✅ 已完成的任务............ ✏️ 🗑 │ │
│ └─────────────────────────────────┘ │
└──────────────────────────────────────┘
```

### 交互细节（不要漏）

| 功能 | 交互方式 |
|------|---------|
| 列表拖拽排序 | `draggable` + `onDragStart/Over/Drop`，松开后调 PUT 更新 `sort_order` |
| 待办编辑 | 点击 ✏️ 进入行内编辑，Enter 保存 / Esc 取消 |
| 待办勾选 | 点击 ○ 切换 done 状态，完成后文字加 `line-through` |
| 想法/学习状态 | 点击状态标签依次流转 |
| 想法/学习编辑 | 点击 ✏️ 弹出完整编辑弹窗（Modal） |
| 想法/学习详情 | 点击标题弹出详情弹窗（只读） |
| 操作按钮 | 默认隐藏 `opacity-0`，hover 行时显示 `group-hover:opacity-100` |
| 输入框回车 | Enter 键快捷提交 |

### 完整页面代码

完整 `page.tsx` 在牛二工程：

```
FeiControl/src/app/(dashboard)/shared/page.tsx
```

复制后改两处：
1. **第 46 行**：`const userId = 1;` → `const userId = 2;`（用于标记谁创建的）
2. **来源标记**：`source_agent: "小牛"` → `source_agent: "小八"`（共 2 处，搜索替换）

### 实时同步机制

页面每 **5 秒自动刷新** 数据，无需手动刷新。一方修改后，另一方最多等 5 秒就能看到。

每条数据旁会显示 🐮（小牛创建）或 🐙（小八创建）的创建者标识。

### 如果不用 Tailwind

页面主要用了这些 Tailwind 类，如果不用 Tailwind 可以写等价 CSS：

- 布局：`flex` `flex-col` `flex-1` `items-center` `justify-between` `gap-{n}`
- 尺寸：`h-full` `w-full` `max-w-2xl` `w-96` `max-h-[80vh]`
- 间距：`p-{n}` `m-{n}` `px-` `py-` `space-y-{n}`
- 文本：`text-sm` `text-xs` `text-lg` `font-medium` `font-bold` `truncate` `line-through`
- 圆角：`rounded` `rounded-lg` `rounded-xl` `rounded-full` `rounded-md`
- 动画：`transition-all` `animate-spin`
- 显示：`overflow-y-auto` `flex-shrink-0` `opacity-0` `group-hover:opacity-100`

---

小八最快 10 分钟就能接上 🐙
